package Decoration
import BaseObject

public class Decoration extends StaticSetupObject
	
	construct(vec2 pos, player owner, int ID, real a)
		super(CreateUnit(owner, ID, pos.x, pos.y, a ),CreateUnit(owner, ID, pos.x, pos.y, a ), 0., CODE_NULL )
