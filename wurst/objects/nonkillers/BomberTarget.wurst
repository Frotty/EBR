package BomberTarget
import Entity
import Terrain
import EventHandling
import Escaper
import SetupObject

constant int ID = 'u004'


public class BomberTarget extends DynamicSetupObject
		
	construct(vec2 pos, player owner)
		super( pos.toVec3(), CreateUnit(owner, ID, pos.x, pos.y, 0.), CreateUnit(owner, ID, pos.x, pos.y, 0.), 0., null )

