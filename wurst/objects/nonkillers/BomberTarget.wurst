package BomberTarget
    import Entity
    import Terrain
    import SetupObject
    import Escaper
    
    public class BomberTarget extends DynamicSetupObject 
        
        construct( real x, real y, unit u, unit setup, real radius )
        	super( x, y, getTerrainZ(x,y), u, setup, 0, function BomberTarget.onInRangeEvent ) 

        override function update()
        	print("yes")
			setup.setX( actor.getX() )
			setup.setY( actor.getY() )


endpackage