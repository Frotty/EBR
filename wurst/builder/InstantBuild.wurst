package InstantBuild
import Builder
import Door
import Revivepoint
import Executer
import Vortex
import Powernode
import Tracks
import Jumper
import Teleporter
import Waypoint
import Spinner
import Unitcreator
import Trap
import Tree
import Texter
import Booster
import Obstacle
import Bombertower
import UnitRemover
import CongaLineHead
import TerrainBlocker
import Fountain
import Decoration
import Pedestal
import PlayerData

public function isOrderBuildOrder( int o ) returns boolean
	int i = 0
	while buildorders[i] != 0
		if buildorders[i] == o
			return true 
		i++
	return false

public function instantBuild(Builder b, vec2 tpos, int id)
	var rdata = getRegionData(tpos)
	if rdata != null and rdata.canBuild(b.owner)
		switch id
			case buildorders[1]
				new Jumper(tpos, b.owner)
			case buildorders[2]
				new RevivePoint(vec3(tpos.x,tpos.y, 0.), b.owner)
			case buildorders[3]
				new Texter(tpos, b.owner)
			case buildorders[5]
				new Bombertower(tpos, b.owner)
			case buildorders[6]
				new Vortex(tpos, b.owner)
			case buildorders[8]
				new Executer(tpos, b.owner)
			case buildorders[11]
				new Booster(tpos, b.owner)
			case buildorders[12]
				new UnitRemover(tpos, b.owner)
			case buildorders[13]
				new Waypoint(tpos, b.owner)
			case buildorders[14]
				new Teleporter(tpos, b.owner)
			case buildorders[15]
				new Door(tpos, b.owner)
			case buildorders[16]
				new SmallObstacle(tpos, b.owner)
			case buildorders[17]
				new BigObstacle(tpos, b.owner)
			case buildorders[18]
				new Tree(tpos, b.owner)
			case buildorders[21]
				new Spinner(tpos, b.owner)	
			case buildorders[20]
				new UnitCreator(tpos, b.owner)
			case buildorders[22]
				new Decoration(tpos, b.owner,id,270.)
			case buildorders[23]
				new Tracks(tpos, b.owner)	
			case buildorders[24]
				new Fountain(tpos, b.owner)	
			case buildorders[25]
				new Powernode(tpos, b.owner)	
			case buildorders[26]
				new Trap(tpos, b.owner)	
			case buildorders[27]
				new Decoration(tpos, b.owner,id,270.)	
			case buildorders[28]
				new Decoration(tpos, b.owner,id,270.)
			case buildorders[29]
				new CongaLineHead(tpos, b.owner)
			case buildorders[30]
				new TerrainBlocker(tpos, b.owner)
			case buildorders[31]
				new VolcanoObstacle(tpos, b.owner)
			case buildorders[32]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[33]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[34]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[35]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[36]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[37]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[38]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[39]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[40]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[41]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[42]
				new Decoration(tpos, b.owner,id,GetRandomReal(0, 360))
			case buildorders[43]
				new MemoryAccessPedestal(tpos, b.owner)
	else
		DisplayTextToPlayer( b.owner, 0, 0, "|cffC51019You have no permission to build in this region!") 

	b.actor.abortOrder()


int array buildorders

@init1 function init_InstantBuild()
	buildorders[0] = 'n00G'
	buildorders[1] = 'n00B' // Jumper
	buildorders[2] = 'n000' // Revivepoint
	buildorders[3] = 'n01B' // Texter
	buildorders[4] = 'n001' 
	buildorders[5] = 'h019' // BomberTower
	buildorders[6] = 'n012' // Vortex
	buildorders[7] = 'h009'
	buildorders[8] = 'n01D'  // Executer
	buildorders[9] = 'n01E'
	buildorders[10] = 'n00F'
	buildorders[11] = 'n00J' // Booster
	buildorders[12] = 'n003' // UnitRemover
	buildorders[13] = 'n004' // Waypoint
	buildorders[14] = 'n00C' // Teleporter
	buildorders[15] = 'h002' // Door
	buildorders[16] = 'obSD' // Small Obstacle
	buildorders[17] = 'obBD' // Big Obstacle
	buildorders[18] = 'n010' // Tree
	buildorders[19] = 'n013'
	buildorders[20] = 'n006' // Unitcreator
	buildorders[21] = 'h01M' // Spinner
	buildorders[22] = 'n00P' // Wall
	buildorders[23] = 'n01C' // Tracks
	buildorders[24] = 'h00H' // Fountain
	buildorders[25] = 'h0nn' // Powernode
	buildorders[26] = 'n00I' // Trap
	buildorders[27] = 'n00Q' // Horizontal Wall
	buildorders[28] = 'n011' // Vertical Wall
	buildorders[29] = 'h000' // Conga Line
	buildorders[30] = 'h009' // TerrainBlocker
	//Decoration
	buildorders[31] = 'n00K' // Volcano
	buildorders[32] = 'n00V' // Lantern
	buildorders[33] = 'n00U' // Mushroom
	buildorders[34] = 'n00Z' // Torch
	buildorders[35] = 'n00T' // Rune
	buildorders[36] = 'n01A' // Suppalight
	buildorders[37] = 'n00M' // Magic Lantern
	buildorders[38] = 'n014' // Farm
	buildorders[39] = 'n00G' // Hay
	buildorders[40] = 'n016' // Obelisk
	buildorders[41] = 'n015' // Poison eggs
	buildorders[42] = 'n017' // Remains
	buildorders[43] = PED_ID // Pedestal
